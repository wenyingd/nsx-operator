---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.0
  creationTimestamp: null
  name: subnetbindings.nsx.vmware.com
spec:
  group: nsx.vmware.com
  names:
    kind: SubnetBinding
    listKind: SubnetBindingList
    plural: subnetbindings
    singular: subnetbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SubnetBinding is the Schema for the parentConfig API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              name:
                description: Name of the "virtualNetwork" or "subnetSet" resource
                  when Type is "virtualNetwork" or "subnetSet". Name is empty if Type
                  is "segments" or "subnets"
                type: string
              segments:
                description: Segments is a list of policy paths for the existing segments.
                  It is set only when Type is "segments".
                items:
                  type: string
                type: array
              subnets:
                description: Subnets is a list of the existing subnets in the cluster.
                  It is set only when Type is "subnets".
                items:
                  type: string
                type: array
              type:
                description: Type of the parent resource of ChildSubnet.
                enum:
                - subnets
                - subnetSet
                - virtualNetwork
                - segments
                type: string
              vlan:
                description: Vlan is the vlan tag configured in the binding. It can
                  be empty, then the handler will choose a valid value based on the
                  existing configurations on the given parent.
                format: int64
                type: integer
            required:
            - type
            type: object
          status:
            description: SubnetBindingStatus defines the observed state of SubnetBinding.
            properties:
              conditions:
                items:
                  description: Condition defines condition of custom resource.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another. This should be when the underlying condition changed.
                        If that is not known, then using the time when the API field
                        changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: Message shows a human-readable message about condition.
                      type: string
                    reason:
                      description: Reason shows a brief reason of condition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type defines condition type.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              vlan:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
